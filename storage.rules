rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /photos/{monthId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.time < timestamp.date(2026, 1, 1);
    }
  }
}
